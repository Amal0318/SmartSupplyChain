# =============================================================================
# Production AI Agent — Backend Dependencies
# =============================================================================

# --- Web Framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1

# --- Database ---
motor==3.6.0          # Async MongoDB driver
pymongo==4.10.1       # Sync MongoDB (used for setup scripts)
beanie==1.27.0        # Async ODM for MongoDB

# --- Authentication & Security ---
python-jose[cryptography]==3.3.0   # JWT tokens
passlib[bcrypt]==1.7.4              # Password hashing
python-multipart==0.0.12            # Multipart form data (file uploads)

# --- Configuration ---
pydantic==2.10.3
pydantic-settings==2.6.1

# --- Data Processing ---
pandas==2.2.3
numpy==2.1.3
openpyxl==3.1.5       # Excel read/write

# --- AI / LLM ---
openai==1.58.1        # OpenAI SDK (abstracted, can swap)
tiktoken==0.8.0       # Token counting

# --- Export ---
reportlab==4.2.5      # PDF generation
jinja2==3.1.5         # Template engine for reports

# --- HTTP Client ---
httpx==0.28.1

# --- Logging ---
structlog==24.4.0

# --- Validation & Utilities ---
python-dateutil==2.9.0
email-validator==2.2.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.28.1          # For TestClient
fakeredis==2.26.2

# --- Development ---
black==24.10.0
isort==5.13.2
flake8==7.1.1
